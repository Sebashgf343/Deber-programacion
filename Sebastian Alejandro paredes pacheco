#include <stdio.h>

int main() {
    int a, b, c;

    printf("Numero inicial: ");
    scanf("%d", &a);

    printf("Numero final: ");
    scanf("%d", &b);

    printf("Incremento: ");
    scanf("%d", &c);

    for (int i = a; i <= b; i = i + c) {
        printf("%d\n", i);
    }

    return 0;
}
